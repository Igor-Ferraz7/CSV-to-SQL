# CSV-to-SQL
Uma ferramenta web para transformar arquivos CSV em comandos SQL prontos para executar. Suporta múltiplos delimitadores, mapeamento automático de tipos de dados, e operações SQL customizáveis (INSERT, UPDATE, DELETE, SELECT, MERGE).
